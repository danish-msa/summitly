# 🎨 Summitly AI - Visual Integration Diagram

## 📐 Complete System Architecture

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE LAYER                              │
│                         (Browser - localhost:3000)                        │
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  /ai - AI Chat Page                                             │    │
│  │  ┌───────────────────────────────────────────────────────────┐ │    │
│  │  │  AiChatAppIntegrated Component                            │ │    │
│  │  │  ┌─────────────────────────────────────────────────────┐ │ │    │
│  │  │  │  - Chat history sidebar                             │ │ │    │
│  │  │  │  - Message thread display                           │ │ │    │
│  │  │  │  - Property cards rendering                         │ │ │    │
│  │  │  │  - Analysis insights display                        │ │ │    │
│  │  │  │  - Message input composer                           │ │ │    │
│  │  │  └─────────────────────────────────────────────────────┘ │ │    │
│  │  └───────────────────────────────────────────────────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────┘
                                  ↕ (React state, user interactions)
┌──────────────────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER (TypeScript)                           │
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  summitly-ai-service.ts                                         │    │
│  │  ┌───────────────────────────────────────────────────────────┐ │    │
│  │  │  SummitlyAIService Class                                  │ │    │
│  │  │  • sendMessage(message, history)                         │ │    │
│  │  │  • searchProperties(params)                              │ │    │
│  │  │  • getPropertyAnalysis(mlsNumber)                        │ │    │
│  │  │  • getLocationInsights(location)                         │ │    │
│  │  │  • searchPreConstruction(query)                          │ │    │
│  │  │  • getInvestmentAnalysis(propertyData)                  │ │    │
│  │  │  • getMarketAnalysis(location, type)                    │ │    │
│  │  │  • calculateMortgage(params)                            │ │    │
│  │  │  • compareProperties(mlsNumbers)                        │ │    │
│  │  │  • Session management                                    │ │    │
│  │  └───────────────────────────────────────────────────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────┘
                                  ↕ (HTTP Fetch API calls)
┌──────────────────────────────────────────────────────────────────────────┐
│                    NEXT.JS API ROUTES (Proxy Layer)                       │
│                                                                            │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐      │
│  │ /api/ai/chat     │  │ /api/ai/search   │  │ /api/ai/analysis │      │
│  │                  │  │                  │  │                  │      │
│  │ • POST handler   │  │ • POST handler   │  │ • POST handler   │      │
│  │ • Request proxy  │  │ • Request proxy  │  │ • Request proxy  │      │
│  │ • Error handling │  │ • Error handling │  │ • Error handling │      │
│  │ • Response parse │  │ • Response parse │  │ • Response parse │      │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘      │
└──────────────────────────────────────────────────────────────────────────┘
                                  ↕ (HTTP POST to localhost:5050)
┌──────────────────────────────────────────────────────────────────────────┐
│                   PYTHON FLASK BACKEND (Port 5050)                        │
│                   voice_assistant_clean.py                                │
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  MAIN ENDPOINTS                                                  │    │
│  │  ┌──────────────────────────────────────────────────────────┐  │    │
│  │  │ POST /api/chat-gpt4                                      │  │    │
│  │  │ POST /api/search-properties                              │  │    │
│  │  │ POST /api/property-analysis                              │  │    │
│  │  │ POST /api/location-insights                              │  │    │
│  │  │ POST /api/openai/market-analysis                         │  │    │
│  │  │ POST /api/openai/investment-analysis                     │  │    │
│  │  │ POST /api/mortgage-calculator                            │  │    │
│  │  │ POST /api/property-comparison                            │  │    │
│  │  │ GET  /health                                             │  │    │
│  │  └──────────────────────────────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  BACKEND SERVICES                                                │    │
│  │  ┌──────────────────────────────────────────────────────────┐  │    │
│  │  │ • listings_service (Repliers API integration)           │  │    │
│  │  │ • nlp_service (Natural language processing)             │  │    │
│  │  │ • openai_service (AI analysis & chat)                   │  │    │
│  │  │ • preconstruction_service (Pre-con properties)          │  │    │
│  │  │ • estimates_service (Property valuations)               │  │    │
│  │  │ • chatbot_formatter (Response formatting)               │  │    │
│  │  │ • saved_search_service (User preferences)               │  │    │
│  │  └──────────────────────────────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────┘
                                  ↕ (API calls)
┌──────────────────────────────────────────────────────────────────────────┐
│                      EXTERNAL APIs & SERVICES                             │
│                                                                            │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐      │
│  │ Repliers API     │  │ OpenAI API       │  │ Exa AI           │      │
│  │                  │  │                  │  │                  │      │
│  │ • MLS Listings   │  │ • GPT-4 Chat     │  │ • Real-time data │      │
│  │ • Property data  │  │ • Analysis       │  │ • Market info    │      │
│  │ • Search         │  │ • Valuations     │  │ • Insights       │      │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘      │
└──────────────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

### Example: User Searches for Properties

```
┌─────────────┐
│    User     │
│ Types Query │
└──────┬──────┘
       │ "Show me condos in Toronto"
       ↓
┌─────────────────────┐
│   AiComposer        │
│   (Input Field)     │
└──────┬──────────────┘
       │ handleSend()
       ↓
┌─────────────────────────────┐
│  AiChatAppIntegrated        │
│  • Add user message to UI   │
│  • Build conversation hist  │
└──────┬──────────────────────┘
       │ summitlyAIService.sendMessage()
       ↓
┌──────────────────────────────┐
│  summitly-ai-service.ts      │
│  • Format request            │
│  • Add session ID            │
└──────┬───────────────────────┘
       │ HTTP POST
       ↓
┌──────────────────────────────┐
│  /api/ai/chat                │
│  • Proxy request             │
│  • Forward to backend        │
└──────┬───────────────────────┘
       │ HTTP POST to :5050
       ↓
┌───────────────────────────────────┐
│  voice_assistant_clean.py         │
│  POST /api/chat-gpt4               │
│  • Parse user intent              │
│  • Detect property search         │
└──────┬────────────────────────────┘
       │ Calls search_repliers_properties()
       ↓
┌───────────────────────────────────┐
│  listings_service                  │
│  • Call Repliers API              │
│  • Parse location "Toronto"       │
│  • Filter property_type "condo"   │
└──────┬────────────────────────────┘
       │ API Response
       ↓
┌───────────────────────────────────┐
│  Repliers API                      │
│  • Search MLS database            │
│  • Return property listings       │
└──────┬────────────────────────────┘
       │ Properties array
       ↓
┌───────────────────────────────────┐
│  voice_assistant_clean.py         │
│  • Standardize property data      │
│  • Add images, format prices      │
│  • Generate AI response text      │
└──────┬────────────────────────────┘
       │ JSON Response
       ↓
┌───────────────────────────────────┐
│  /api/ai/chat (proxy)              │
│  • Parse response                 │
│  • Return to frontend             │
└──────┬────────────────────────────┘
       │ HTTP Response
       ↓
┌───────────────────────────────────┐
│  summitly-ai-service.ts            │
│  • Parse properties                │
│  • Extract analysis                │
└──────┬────────────────────────────┘
       │ Structured data
       ↓
┌───────────────────────────────────┐
│  AiChatAppIntegrated               │
│  • Create assistant message       │
│  • Add properties to UI           │
│  • Update chat state              │
└──────┬────────────────────────────┘
       │ React setState()
       ↓
┌───────────────────────────────────┐
│  AiThread (UI Component)           │
│  • Render properties as cards    │
│  • Show analysis insights         │
│  • Display to user                │
└───────────────────────────────────┘
```

## 🎯 Component Hierarchy

```
App
└── AiChatAppIntegrated
    ├── AiNoScroll (prevents body scroll)
    ├── AiTopBar
    │   ├── New Chat Button
    │   ├── History Toggle
    │   └── Dataset Preview Toggle
    ├── Content Area (Flex Container)
    │   ├── AiSidebar (Chat History)
    │   │   └── Chat List Items
    │   ├── Main Chat Area
    │   │   ├── AiChatLanding (initial state)
    │   │   │   └── Sample prompts
    │   │   └── AiThread (active chat)
    │   │       ├── User Message Bubbles
    │   │       └── Assistant Message Bubbles
    │   │           ├── Intro Text
    │   │           ├── Section Bullets
    │   │           ├── Property Cards (NEW)
    │   │           ├── Analysis Insights (NEW)
    │   │           └── Outro Text
    │   └── DatasetPreviewPanel (optional)
    └── AiComposer (Message Input)
        ├── Attachment Button
        ├── Text Input Field
        └── Send Button
```

## 📊 Type System Flow

```
TypeScript Types (Frontend)
├── AiChat
│   ├── id: string
│   ├── title: string
│   ├── createdAt: string
│   ├── updatedAt: string
│   └── messages: AiChatMessage[]
│
├── AiChatMessage
│   ├── id: string
│   ├── role: 'user' | 'assistant'
│   ├── createdAt: string
│   ├── userText?: string
│   └── assistant?:
│       ├── intro: string
│       ├── sections: Section[]
│       ├── outro?: string
│       ├── properties?: AiProperty[]  ← NEW
│       └── analysis?: AiAnalysis      ← NEW
│
├── AiProperty (NEW)
│   ├── mls_number?: string
│   ├── address?: string
│   ├── price?: number
│   ├── bedrooms?: number
│   ├── bathrooms?: number
│   ├── sqft?: number
│   ├── property_type?: string
│   └── images?: string[]
│
└── AiAnalysis (NEW)
    ├── estimated_value?: { low, mid, high }
    ├── neighborhood_summary?: string
    ├── schools?: string
    ├── connectivity?: string
    ├── market_trend?: string
    ├── rental_potential?: string
    ├── investment_grade?: string
    └── final_recommendation?: string

          ↕ (API Communication)

Python Types (Backend)
├── ChatResponse (Dict)
│   ├── success: bool
│   ├── response: str
│   ├── properties?: List[Dict]
│   ├── analysis?: Dict
│   └── session_id?: str
│
└── PropertyData (Dict)
    ├── Standardized fields
    ├── from standardize_property_data()
    └── Compatible with frontend types
```

## 🔐 Security Flow

```
User Request
    ↓
Frontend (Browser)
    ↓ [CORS Headers]
Next.js API Route (Server-side)
    ↓ [Server-to-Server]
Python Backend
    ↓ [API Keys in .env]
External APIs (Repliers, OpenAI)
    ↓ [Authenticated Responses]
Python Backend
    ↓ [Data Sanitization]
Next.js API Route
    ↓ [Type Validation]
Frontend (Browser)
    ↓
User sees safe, validated data
```

## 📈 State Management

```
React State in AiChatAppIntegrated:

┌─────────────────────────────────┐
│ chats: AiChat[]                 │ ← All chat conversations
├─────────────────────────────────┤
│ activeChatId: string            │ ← Currently selected chat
├─────────────────────────────────┤
│ draft: string                   │ ← Input field text
├─────────────────────────────────┤
│ historyOpen: boolean            │ ← Sidebar visibility
├─────────────────────────────────┤
│ isProcessing: boolean           │ ← Loading state (NEW)
└─────────────────────────────────┘

Actions:
• handleSend(text) → Sends message to backend
• handleNewChat() → Creates new conversation
• handleDeleteChat(id) → Removes conversation
• setDraft(text) → Updates input field
```

## 🎨 UI State Transitions

```
Initial State:
┌────────────────┐
│ AiChatLanding  │ → Shows sample prompts
└────────────────┘

User sends first message:
┌────────────────┐
│ Create new chat│
│ Add user msg   │
│ Show loading   │ (isProcessing = true)
└────────────────┘

Backend responds:
┌────────────────┐
│ Parse response │
│ Add properties │
│ Add analysis   │
│ Update UI      │ (isProcessing = false)
└────────────────┘

Active Chat State:
┌────────────────┐
│ AiThread       │ → Shows conversation
│ + AiComposer   │ → Input field
└────────────────┘
```

---

This visual guide shows exactly how all the pieces fit together! 🎉
