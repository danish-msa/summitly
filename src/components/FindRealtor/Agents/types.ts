/**
 * URL params for Find an Agent results (from path segments).
 */
export interface FindRealtorAgentsParams {
  search: string;
  intent: string;
  sort: string;
  agenttype: string;
  page: string;
}
