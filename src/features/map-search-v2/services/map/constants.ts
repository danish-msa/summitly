export const MAP_CONSTANTS = {
  // When count is large, render cluster markers instead of individual markers.
  API_COUNT_TO_ENABLE_CLUSTERING: 500,
  // Only show markers once you're zoomed in enough (prevents DOM explosion).
  MIN_ZOOM_FOR_MARKERS: 12,
  // Cluster marker bounds padding fraction.
  ZOOM_TO_MARKER_BUFFER: 0.12,
};

